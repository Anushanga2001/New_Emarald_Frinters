namespace Backend.Domain.Enums
{
    public enum NotificationType
    {
        QuoteCreated = 5,
        General = 6,
        ContactMessage = 7,
        QuoteApproved = 8,
        QuoteRejected = 9,
        RatingSubmitted = 10
    }
}
