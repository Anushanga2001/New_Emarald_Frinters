# Used by Vite at BUILD time on Vercel.
# IMPORTANT: After changing values here, you must REDEPLOY on Vercel
# (env vars are baked into the JS bundle at build time).
#
# Set the same VITE_API_URL in Vercel -> Settings -> Environment Variables
# (this file is a fallback / documentation; Vercel env vars override it).

VITE_API_URL=https://newemarald.runasp.net/api
